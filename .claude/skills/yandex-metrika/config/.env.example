# Copy to config/.env (git-ignored) to scope a token to this skill only.
# Otherwise the skill falls back to the repo-root .env.

YANDEX_METRIKA_TOKEN=
# Optional default counter for ad-hoc calls:
YANDEX_METRIKA_COUNTER_ID=32273834
